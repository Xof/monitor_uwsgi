[project]
name = "monitor-uwgi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Christophe Pettus", email = "cpettus@pgexperts.com" }
]
requires-python = ">=3.13"
dependencies = [
    "click>=8.3.1",
    "datadog>=0.52.1",
]

[project.scripts]
monitor_uwsgi = "monitor_uwsgi.monitor_uwsgi:main"

[build-system]
requires = ["uv_build>=0.9.7,<0.10.0"]
build-backend = "uv_build"
